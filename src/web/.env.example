# Required: Supabase project URL for authentication and database access
# Format: Must be a valid HTTPS URL from your Supabase project settings
# Security Level: Public
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Required: Supabase anonymous key for public API access
# Format: Must be a valid Supabase anon key from your project settings
# Security Level: Public
# Used for: Initial authentication and public data access
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# API Configuration
# Required: Base URL for backend API endpoints
# Format: Must be a valid URL with /api/v1 suffix
# Security Level: Public
# Used for: All backend API communications
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1

# Site Configuration
# Required: Public URL of the web application
# Format: Must be a valid URL
# Security Level: Public
# Used for: Authentication callbacks and absolute URL generation
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Vercel Deployment Configuration
# Optional: Automatically set in Vercel environment
# Format: Valid URL string
# Security Level: Public
# Used for: Production deployment URL detection
NEXT_PUBLIC_VERCEL_URL=${VERCEL_URL}

# Environment Configuration
# Required: Current environment name
# Format: Must be one of: development, staging, production
# Security Level: Public
# Used for: Environment-specific behavior and error handling
NEXT_PUBLIC_ENVIRONMENT=development